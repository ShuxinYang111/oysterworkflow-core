# OpenAI-compatible LLM API key used by extract-skill-llm and llm smoke tests.
OPENAI_API_KEY=

# Optional: set to 1 only when intentionally debugging rendered LLM prompts.
TRACE_LLM_INPUT=0

# Optional: set to 1 to include per-call timing details in local traces.
TRACE_LLM_TIMING=0
